echo "Running build check before commit..."
pnpm run build || (echo "Build failed. Please fix the errors before committing." && exit 1)
vercel --prod || (echo "Deploy failed. Something went wrong." && exit 1)
